# Dependencies
node_modules/
.pnp
.pnp.js

# Build
.next/
out/
build/
dist/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
ENV/
env.bak/
venv.bak/

# Data & Cache (User-specific, contains personal paths and API keys)
data/config.json
data/embedding_cache.json
data/conversation_cache.json
data/vector_db_sync_state.json
data/idea_bank.json
data/insight_bank.json

# Test Results (may contain personal paths)
e2e/
e2e-results/
playwright-report/
playwright.config.ts
test-results/

# AI Documentation (Mode A - canonical docs are now published to GitHub)
# Canonical docs (README.md, CLAUDE.md, Plan.md, BUILD_LOG.md, PIVOTS.md, ARCHITECTURE.md) are tracked
data/items_bank.json
data/items_bank_backup_*.json

# Lenny Podcast Archive
# Raw transcripts (cloned from GitHub) - not needed, we ship pre-computed embeddings
data/lenny-transcripts/
# NOTE: lenny_embeddings.npz and lenny_metadata.json are COMMITTED
# so new users get expert perspectives immediately (zero setup, zero cost)
