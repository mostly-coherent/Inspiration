# Dependencies
node_modules/
.pnp
.pnp.js

# Build
.next/
out/
build/
dist/

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env*.local
.env

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
/env/
/venv/
/ENV/
/env.bak/
/venv.bak/

# Data & Cache (User-specific, contains personal paths and API keys)
data/config.json
data/embedding_cache.json
data/conversation_cache.json
data/vector_db_sync_state.json
data/idea_bank.json
data/insight_bank.json
data/kg_indexing_progress.json

# Test Results (may contain personal paths)
e2e/
e2e-results/
playwright-report/
playwright.config.ts
test-results/

# AI Documentation (Mode A - canonical docs are now published to GitHub)
# Canonical docs (README.md, CLAUDE.md, Plan.md, BUILD_LOG.md, PIVOTS.md, ARCHITECTURE.md) are tracked
data/items_bank.json
data/items_bank_backup_*.json

# Lenny Podcast Archive
# Raw transcripts (cloned from GitHub)
data/lenny-transcripts/
# Pre-computed embeddings (downloaded from GitHub Releases on first run)
# Too large for git (219MB + 28MB), but auto-downloaded via scripts/download-lenny-embeddings.sh
data/lenny_embeddings.npz
data/lenny_metadata.json
# Exported KG files (distributed via GitHub Releases, not committed)
exports/lenny-kg/*.json
# Indexing logs (local only, may contain user paths)
data/lenny_index.log
data/*.log

# Performance logs (user-specific run logs, grows unbounded over time)
data/performance_logs/

# User-generated data (specific to each user's usage)
data/saved_reflections.json
data/theme_map.json
data/themes.json
data/use_cases_output/

# Engine debug output (failed parse artifacts)
engine/data/output/

# Archive folder (historical planning docs, not needed for app functionality)
_archive/

# Knowledge Graph Documentation
# INSPIRATION_V2_PLAN.md - Future roadmap (kept for reference)

# Knowledge Graph Monitoring Scripts (local development tools, not needed by users)
monitor-kg-build.sh
monitor_kg_indexing.py
check_kg_progress.py
check_kg_status_simple.py
monitor_lenny_kg.sh
check-lenny-status.sh

# Example/Test Files (not needed for production)
inspiration.spec.ts.example
playwright.config.ts.example